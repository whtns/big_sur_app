# Sample Nginx configuration for reverse proxy
# Save as: nginx.conf

events {
    worker_connections 1024;
}

http {
    upstream bigsur_web {
        server web:8050;
    }

    # Gzip compression — reduces Plotly.js/React bundles by ~70%
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        image/svg+xml;

    # HTTP to HTTPS redirect (uncomment for production)
    # server {
    #     listen 80;
    #     server_name _;
    #     return 301 https://$host$request_uri;
    # }

    # HTTP server (development)
    server {
        listen 80;
        server_name _;

        client_max_body_size 1000M;  # Support large file uploads

        location / {
            proxy_pass http://bigsur_web;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # For Dash/Flask streaming
            proxy_buffering off;
            proxy_request_buffering off;

            # WebSocket support (if needed)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # Dash component bundles — fingerprinted URLs, safe to cache indefinitely
        location /_dash-component-suites/ {
            proxy_pass http://bigsur_web/_dash-component-suites/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }

        # App static assets
        location /assets/ {
            proxy_pass http://bigsur_web/assets/;
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # HTTPS server (production - uncomment and configure)
    # server {
    #     listen 443 ssl http2;
    #     server_name your-domain.com;
    #
    #     ssl_certificate /etc/nginx/certs/cert.pem;
    #     ssl_certificate_key /etc/nginx/certs/key.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;
    #     ssl_prefer_server_ciphers on;
    #
    #     client_max_body_size 1000M;
    #
    #     location / {
    #         proxy_pass http://bigsur_web;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto https;
    #         
    #         proxy_buffering off;
    #         proxy_request_buffering off;
    #         
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #     }
    # }
}
